@extends('layouts.app', ['portal' => 'admin', 'preview' => false, 'title' => 'Create Student Account', 'pageHeading' => 'Create Student Account'])
@section('content')
<div class="page-intro"><div><span class="eyebrow">Administrator portal</span><h1>Create Student Account</h1><p>Assign a unique Student ID and secure initial password.</p></div><a class="button button-secondary" href="{{ route('admin.students.index') }}">Back to students</a></div>
<div class="account-form-layout"><form class="content-panel account-form" method="POST" action="{{ route('admin.students.store') }}">@csrf<div class="form-grid"><label>Full name<input name="name" value="{{ old('name') }}" required autocomplete="name">@error('name')<small class="field-error">{{ $message }}</small>@enderror</label><label>Student ID<input name="student_id" value="{{ old('student_id') }}" required autocomplete="off" placeholder="e.g. CCS-2026-001">@error('student_id')<small class="field-error">{{ $message }}</small>@enderror</label><label class="form-span">Email address<input name="email" type="email" value="{{ old('email') }}" required autocomplete="email">@error('email')<small class="field-error">{{ $message }}</small>@enderror</label><label>Password<input name="password" type="password" required autocomplete="new-password">@error('password')<small class="field-error">{{ $message }}</small>@enderror</label><label>Confirm password<input name="password_confirmation" type="password" required autocomplete="new-password"></label></div><button class="button button-primary button-full" type="submit">Create Student Account</button></form><aside class="content-panel account-guidance"><span class="eyebrow">Account security</span><h2>Initial access</h2><p>Share the Student ID and initial password with the student through an approved private channel.</p><ul><li>The Student role is stored on the server.</li><li>Passwords are hashed before storage.</li><li>Student IDs and email addresses must be unique.</li><li>Public Student registration remains unavailable.</li></ul></aside></div>
@endsection
