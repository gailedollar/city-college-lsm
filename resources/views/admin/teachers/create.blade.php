@extends('layouts.app', ['portal' => 'admin', 'preview' => false, 'title' => 'Create Teacher Account', 'pageHeading' => 'Create Teacher Account'])
@section('content')
<div class="page-intro"><div><span class="eyebrow">Administrator portal</span><h1>Create Teacher Account</h1><p>Assign a unique Employee ID and secure initial password.</p></div><a class="button button-secondary" href="{{ route('admin.teachers.index') }}">Back to teachers</a></div>
<div class="account-form-layout"><form class="content-panel account-form" method="POST" action="{{ route('admin.teachers.store') }}">@csrf<div class="form-grid"><label>Full name<input name="name" value="{{ old('name') }}" required autocomplete="name">@error('name')<small class="field-error">{{ $message }}</small>@enderror</label><label>Employee ID<input name="employee_id" value="{{ old('employee_id') }}" required autocomplete="off" placeholder="e.g. CCT-2026-001">@error('employee_id')<small class="field-error">{{ $message }}</small>@enderror</label><label class="form-span">Email address<input name="email" type="email" value="{{ old('email') }}" required autocomplete="email">@error('email')<small class="field-error">{{ $message }}</small>@enderror</label><label>Password<input name="password" type="password" required autocomplete="new-password">@error('password')<small class="field-error">{{ $message }}</small>@enderror</label><label>Confirm password<input name="password_confirmation" type="password" required autocomplete="new-password"></label></div><button class="button button-primary button-full" type="submit">Create Teacher Account</button></form><aside class="content-panel account-guidance"><span class="eyebrow">Account security</span><h2>Initial access</h2><p>Share the Employee ID and initial password with the teacher through an approved private channel.</p><ul><li>The Teacher role is stored on the server.</li><li>Passwords are hashed before storage.</li><li>Employee IDs and email addresses must be unique.</li><li>Public teacher registration remains unavailable.</li></ul></aside></div>
@endsection
